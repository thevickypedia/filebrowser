# todo: compose and docker run with -v doesn't have access to run.sh
services:
  filebrowser:
    image: filebrowser
    container_name: filebrowser
    ports:
      - 8080:80
    volumes:
      - ${DOCKER_VOLUME_STORAGE:-.}:/opt/filebrowser
    restart: unless-stopped
