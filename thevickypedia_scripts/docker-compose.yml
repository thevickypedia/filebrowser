services:
  filebrowser:
    image: filebrowser
    container_name: filebrowser
    ports:
      - 8080:80
#    volumes:
#      - ${DOCKER_VOLUME_STORAGE:-/.}/filebrowser/data:/data
#      - ${DOCKER_VOLUME_STORAGE:-/.}/filebrowser/config:/config
    environment:
      - FB_BASEURL=/filebrowser
    restart: unless-stopped
